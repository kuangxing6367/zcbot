# ============================================================
# 核心依赖（SQLite 模式必须）
# ============================================================
websockets>=12.0
apscheduler>=3.10.0
flask>=2.3.0
flask-cors>=4.0.0
pyyaml>=6.0
bcrypt>=4.0.0
requests>=2.28.0
psutil>=5.9.0
Pillow>=10.0.0
numpy>=1.24.0

# ============================================================
# MySQL 可选依赖（使用 MySQL 数据库时需要）
# ============================================================
pymysql>=1.0.0
DBUtils>=3.0.0